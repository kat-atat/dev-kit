{
  "name": "dev-kit",
  "version": "1.0.1",
  "description": "develop tools for client-side",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --mode production --entry ./src/dev-kit.js --output ./dist/dev-kit.js",
    "dev": "webpack --mode development --entry ./src/dev-kit.js --output ./dist/dev-kit.js --watch",
    "prepare": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kat-atat/dev-kit.git"
  },
  "files": [
    "src",
    "dist",
    "index.html"
  ],
  "license": "ISC",
  "devDependencies": {
    "hyperapp": "^1.2.9",
    "webpack": "^4.29.5",
    "webpack-cli": "^3.2.3"
  }
}
